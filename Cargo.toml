[package]
name = "frust"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2.185"
pci-ids = "0.2.6"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
